1. Docker is used to standardize the environment.
2. Build once (the ML model) & deploy it anywhere.
3. in VMs we cannot assign resources of VM to another, whereas in docker we can frm docker to  another. therefore it has 2 properties:- Isolation    & Portability